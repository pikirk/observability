name: obserability-dashboard-newrelic
run-name: Dashboard build - ${{ github.actor }}
on: [push]
jobs:
  build-dashboard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Lint
        uses: dflook/terraform-fmt-check@v1
        id: tf-lint-result
        with:
          path: ${{ github.workspace }}/terraform/stacks/

      - name: Check Lint Result
        if: ${{ failure() && steps.tf-lint-result.outputs.failure-reason == 'check-failed' }}
        run: | 
          echo "Terraform formatting check failed"
          exit 1

      - name: Setup Terraform Workspace
        run: |
          cd ${{ github.workspace }}/terraform/stacks/dash-endpoints/
          terraform version
          terraform init
          terraform workspace new prod
          terraform workspace select prod
          terraform workspace list

      - name: Terraform Plan
        id: tf-plan-result
        run: |
          cd ${{ github.workspace }}/terraform/stacks/dash-endpoints/
          terraform plan \
            -var-file=./configurations/prod.tfvars \
            -var nr_account_id="{{ secrets.TF_NR_ACCOUNT }}" -var nr_api_key="{{ secrets.TF_NR_API_KEY }}" \
            -out=${{ github.workspace }}/launrdy-dash.tfplan

      - name: Check Plan Result
        if: ${{ failure() }}
        run: | 
          echo "Terraform plan failed"
          exit 1 

